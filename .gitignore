# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
test-results
server/data/auth-config.json
server/data/tls/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
skills/

# graphify knowledge graph (local only)
graphify-out/
.playwright-mcp/
*.local.mts

# rebuild orchestrator transient state
infra/.rebuild-state

# Local acceptance/planning artifacts — never pushed
jira/

# Extracted blobs / local artifacts (not for commit)
output/

.next/
*.tsbuildinfo
.env
.env.*
!.env.example
coverage/

# Runtime state (regenerated at startup; never commit)
server/data/engine-targets.json

# Local agent/project conventions — not for the public repo
CLAUDE.md
